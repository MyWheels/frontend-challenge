import { SearchParams } from '../types'

export const searchParamsMock: SearchParams = {
  filter: {},
  locationPoint: {
    latitudeMin: 48,
    latitudeMax: 56,
    longitudeMin: 1,
    longitudeMax: 9,
  },
}
